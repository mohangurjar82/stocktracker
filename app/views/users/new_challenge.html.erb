<div class="window-height">
	<%= render 'layouts/navigation'  %>
	<div class="container">
		<div class="row">
			<h1>challenge</h1>
			<%= form_tag(new_challenge_user_path(@user), :class=>"form-horizontal") do %>
					<% @user.stocks.each do |stock| %>
						<div class="form-group">
					    <label class="control-label col-sm-2" for="email"><%= stock.name %>:</label>
					    <div class="col-sm-10">
					    	<%= check_box_tag 'stock_ids[]', stock.id , :class => "form-control" %>
					    </div>
					  </div>
					<% end %>

				  <div class="form-group"> 
				    <div class="col-sm-offset-2 col-sm-10">
				      <button type="submit" class="btn btn-primary">Submit</button>
				    </div>
				  </div>

			<% end %>
		</div>
					<p>Please select atleaset 5 stocks </p>
	</div>
</div>

<%= form_tag(new_challenge_user_path(@user)) do %>
	<% @user.stocks.each do |stock| %>
		<%= stock.name %><%= check_box_tag 'stock_ids[]', stock.id %><br>
	<% end %>
	<%= submit_tag "Done" %>
<% end %>